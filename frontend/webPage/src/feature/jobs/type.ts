export type jobInfo = {
  id: string;
  title: string;
  status: string;
  url: string;
  applicantId: string;
};

export type jobUser = {
  mail: string;
  clerkId: string;
};

export type applicant = {
  id: string;
  name: string;
};
